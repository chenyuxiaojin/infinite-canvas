{
  "name": "infinite-canvas-desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-desktop.mjs",
    "stage": "node scripts/prepare-desktop.mjs",
    "build:app": "tauri build --bundles app && node scripts/install-local.mjs",
    "install:app": "node scripts/install-local.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
