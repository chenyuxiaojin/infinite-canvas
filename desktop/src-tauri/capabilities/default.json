{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "main-loopback",
  "description": "Allow only the fixed local canvas origin to use non-shell Tauri core APIs.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:3100/*"]
  },
  "permissions": ["core:default", "desktop-runtime"]
}
