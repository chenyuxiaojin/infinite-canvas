[package]
name = "infinite-canvas-external-connectors"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2.12", default-features = false }

[dev-dependencies]
pretty_assertions = "1.4"

[[bin]]
name = "external-connectors-probe"
path = "src/bin/probe.rs"
