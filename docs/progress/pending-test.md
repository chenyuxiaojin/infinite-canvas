---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

产品目标和通过标准统一见 [产品需求与验收标准](../overview/product-requirements.md)。用户已授权全部修复与 Rust 迁移、Antigravity 画布窄许可及两家各一条只读模型验收；下文保留历史实现状态，实际是否执行以最新专项证据为准，不重复申请或重复消费。

## 本轮正式安装与剩余验收

- Rust 业务 API、保存/素材修复与七项优化已统一构建并安装。正式三服务、数据读回、保存精度误报修复和 51 组参数转译抽验通过；137 项统一前端回归通过，生产构建执行全量类型检查。安装前后证据和 R1–R8 矩阵见 [整合验收](canvas-rust-repair.md)。
- 新独立项目已完成正式 ZIP 导入/导出、原图全屏抽验、短视频播放、历史差异及两次恢复、单节点撤销重做、视口/面板正常关闭重启。音频仅播放器进度通过，声音输出未采集。精确对象与剩余条件见 [原生续验](canvas-native-acceptance.md)，原用户项目始终未回滚。
- 本轮正式文件选择和保存流程已完成：34 份混合素材共 427,820,853 字节，原生导出 SHA-256 全部一致；416 处稳定引用含嵌套历史映射一致。正文节点和连线保留，详见原生续验报告。
- 新增原生通过：28条聊天跨页/全文搜索/复制；多选成组、新连线、越界和撤销重做；并发拒绝、另存副本、模拟忙碌恢复保护及恢复后重启；70%/85%/未知上限和SOP来源指纹显示。原10条项目和65份原素材保持。Kling40项真实组件隔离渲染、真实原图音视频URL生命周期、刷新/忙碌定向7项通过。剩余核心问题为CUA拖动buttons=0触发松手保护（正式诊断已定位），不能代替正常鼠标持续平移实测；Kling正式布局、WebKit回收及帧时间/可听音频/额外真模型另列，详见[原生续验](canvas-native-acceptance.md)。

## 画布侧栏 Grok / Antigravity（已安装，真实验证范围有限）

- Rust 接入官方本机 CLI 和既有登录；来源分开保存会话，校验归属，支持串行画布工具、流式文字、停止/断线和重复请求保护。图片附件明确拒绝，不静默丢图。
- Grok 一条真实只读提示成功调用一次画布摘要工具，正确返回标题和 48 节点。此专项使用真实 CLI 与只读 MCP 快照，不能代替正式 UI 写入验收。
- Antigravity 的窄 MCP 许可已经按授权备份、写入并读回；原单次模型提示因 Agent 发现问题失败。之后已修复定义发现、MCP 地址和恢复，零模型初始化/恢复通过，真实工具调用未再复验。
- 两家各一条授权已使用，不再次消耗模型或媒体额度。正式连续恢复、取消、权限交互与端到端工具能力的剩余范围见 [接入记录](canvas-local-agent-integration.md)。

## 历史验收记录

下列记录描述对应旧轮次，旧 Go 服务、未修类型错误及旧测试数量不代表本轮状态；以顶部整合验收为准。

## 画布侧栏本机 Codex 试接（已安装，真实只读首测通过）

- 新增“本机 Codex / API 模型”来源切换；切换来源创建独立对话，不把 API 历史冒充 Codex 会话。原 API 和普通终端保留。
- 使用官方 `codex app-server`、ChatGPT 现有登录及动态画布工具；会话 ID 持久化，并由桌面应用的项目/对话登记校验归属。只认已明确绑定的片子目录，找不到或存在重复绑定时停止，不按标题猜测。
- Codex 连接使用只读沙箱、禁用命令环境、Shell、外部 MCP、插件、应用、生命周期 hooks、浏览器/电脑操作、内置生图和其他 Agent；这些仅是此子进程/会话的覆盖值，不修改 Codex 配置或登录文件。前端不接收账号邮件、凭据或完整配置，不开放任意 JSON-RPC 方法。
- 复用原画布工具；删除和媒体生成由画布显式确认，每个工具调用 ID 只执行一次。模型错误不由画布自动重发，停止后拒绝后续排队操作；关闭连接回收自己创建的进程，不影响用户其他 Codex。
- 首次上下文只带选中/已确认/参考节点（最多 16 个）；整体读取先给节点索引，正文按需取。显示最近一次输入加输出相对于模型窗口的近似比例；未返回窗口时不猜百分比。70%/85% 提示仅为本客户端阈值，不是平台限制；原生压缩事件单独展示。流式文字按 100 ms 合并回写，避免每个 token 保存整个画布。
- 已验证无模型请求的真实协议兼容性：CLI 0.153.2，ChatGPT 登录，返回配置模型 `gpt-5.6-sol`、只读/无网络沙箱，动态画布工具注册通过，3 个已有 MCP 禁用后没有启动事件。未调用 `turn/start`，不等同于模型实际回复或原生画布操作已通过。
- 自动化与类型检查：新前端运行时测试、Rust 传输/权限/会话归属/敏感信息过滤/自有进程回收测试通过；全量 Rust 当前 43 通过、1 个原图实机测试按规则跳过。全量 TypeScript 仍有 8 个原有诊断，本次涉及的前端文件无新增诊断。详细测试入口 `canvas-codex.test.mjs` 和 Rust `canvas_codex::tests`。
- 用户回复“做”后已备份、构建、签名、替换并重启唯一正式 App。案例 4 侧栏“本机 Codex”用现有 ChatGPT 登录、配置模型 `gpt-5.6-sol` 完成 1 条只读消息；原生记录显示仅一次 `get_canvas_summary`，实际答复正确报告标题及 48 个节点。该消息内部有 2 次模型请求，最终上下文 24,314 / 258,400，UI 显示约 9%，不是额度百分比。
- 本轮补上异常断线时立即中止并使旧操作确认失效，新增晚到确认不得写入的回归；前端 Codex 14 项与本地工作流 11 项共 25 项通过。已安装包含此修复，但不将隔离测试冒充真实生成/删除流程验收。
- 前后 SQLite 完整性均为 ok、结构一致；18 张非画布表逐行相同，10 张画布原节点/连线全部不变，只有案例 4 的 activeChatId/chatSessions/updatedAt 变化。65 个媒体目录文件、4 个项目绑定、4 个项目 Codex 配置及全局 Codex 配置共 74 个哈希一致；WebKit 已备份，未逐项对账。详细证据和回退位置见 [Codex 安装验收](canvas-codex-install-acceptance.md)。
- 待测：选中图片引用、连续两轮恢复、真实运行中取消、压缩提示和生成/删除确认。本轮未生成媒体、未改节点/连线，未修改账号登录文件；本机运行的是连接程序，不是离线本地大模型。Codex 曾报告技能目录描述因上下文预算被缩短，因此 9% 包含指令/工具/技能等，不只是画布内容；尚未精简全局技能列表。

## 本地工作流收敛（已正式安装）

- 已移除首页/画布的 App 登录、注册、Linux.do 登录、账户菜单、算力点余额及管理后台入口。旧 `/login`、`/admin` 地址回到首页，旧 `/asset-library` 回到“我的素材”，不再挂载账号页面。
- 已移除画布侧栏、底部工具栏和各素材选择器中的服务端素材库；新建节点菜单改为选择“我的素材”。本机素材的添加、拖入、选用、下载和生成结果收藏保留。
- 设置页只提供用户自配 AI 渠道、模型和本机偏好；移除账号云端渠道、S3/R2/WebDAV 设置、配置自动同步及账号容量统计。移除图片/视频结果和画布图片/视频/音频节点的云存储上传入口及处理函数。新导入图片与素材音视频默认保存到本机，旧存储配置不清空，已有素材读取链路保留。
- App 会话不再读取或写入旧账号存储，启动/打开配置不再请求当前账号、账号配置，也不再启用画布、素材、历史、工作流及日志的账号同步。旧账号图片/音频任务停止自动轮询；视频创作台跳过带账号标识的历史任务，画布只恢复明确匹配用户自配渠道的视频任务。保留旧任务状态，不将它们改成失败或删除。
- 依赖 App 账号的“AI 创建工作流”和公开发布选择入口已移除；本地模板编辑、复制、执行、多图提示词生成保留。此项不影响画布助手、普通终端或用户自行使用的 AI 工具。
- 保留本机 Go/Node API、SQLite、Tauri IPC、Agent Bridge 鉴权与凭据、明确项目绑定、普通终端、local-ref 原图读取、提示词在线目录搜索/预览/按需加载/主动收藏，以及用户自配的外部 AI API。未删除共享后端或账号数据库表，未修改 AI 工具登录、版本、应用名或 Git 远端。
- 只读清点时，正式数据库仍有 `users` 1 行、`assets` 38 行、`creative_workflows` 1 行、`user_configs` 0 行、`storage_objects` 0 行，画布 10 行。账号/服务端独有记录仍留在原表，本次未搬到本机素材库，也未删除；浏览器旧账号、存储配置、素材和收藏未清理。这是只读数量清点，不冒充全库逐字段或 IndexedDB Blob 对账。
- 实际验证：`node --test docs/progress/local-workflow.test.mjs docs/progress/canvas-local-image.test.mjs` 共 43 项通过（新增 11 项，原图回归 32 项）。使用真实前端模块与隔离存储/网络/IPC 替身，覆盖旧会话不读写、账号请求停止、旧渠道偏好回到用户 API、配置完成、快捷键保留、本机素材字节、画布 IPC 保存和旧任务保留。未启动模型、未写正式数据；不等同于原生 WebKit 验收。
- 用户回复“允许”后，已完成构建、签名、正式替换与重启；保持唯一「小陈的画布 1.0.0」。安装器两次实际回滚后，准确无登记路径的注销处理及大清单读取上限已修正，新增 5 项回归通过。Next 按现有配置跳过全量类型检查，未提交、推送或发布；完整证据见 [本地工作流安装验收](local-workflow-install-acceptance.md)。
- 原生已验证：首页/设置/图片浮层/素材选择器无账号及云存储入口；生图/视频/工作流打开、重启无登录错误；案例 4 原图抽验、终端 pwd、三项目 MCP、提示词 1,636 条目录与搜索/本地全文读取正常。18 张非画布表完全相同，10 个画布所有节点/连线保留，仅案例 4 视口/时间变化；71 个原文件、5 条 LocalStorage 记录不变，IndexedDB 素材/工作流/历史及现有 Blob 不变。远程全文与新收藏本轮未重测，原生全程请求未抓包，账号零请求由隔离测试验证；未运行 AI 生成或消耗额度。

## 上一轮已安装状态

- 2026-09-04，图片读取 → 项目旧路径/应用登记 → 终端背压均已按顺序修复并安装到正式「小陈的画布 1.0.0」。最终安装、数据读回和原生证据见 [顺序修复验收](ordered-repair-acceptance.md)，下列历史专项结论不等于全部功能验收。
- 前端 60 项、Rust 37 项通过，另 1 个真实图片 opt-in 默认跳过（32 张已另行验证）。原四文件八处 TypeScript 错误保持未跨范围修改。
- 原生 64 MiB 输出从首轮背压版 98.43 秒降到累计确认版 7.01 秒，无错；持续输出输入、拖面板、关闭清理、重开、退出和重启均实测。不是全画布 FPS 或模型响应提速。
- 最终 18 张非画布表完全相同、10 个画布无增删、所有节点/连线不变；案例 4 只有定位视口/更新时间，QA 只有更新时间变化，三个片子绑定逐字未变。

## 本地原图显示修复（顺序修复第 1 步）

- 已按用户授权完成图片源码修复并正式安装；实际人物/场景原图及杰克放大预览已在原生 App 抽验。详见 [图片修复记录](local-ref-image-fix.md) 与最终顺序验收。
- 仅通过当前画布 ID + 已登记素材 key 请求原图；后端校验准确保存的片子绑定，从实际 App 数据目录共享 `agent-media` 根读取该项目登记文件，不按标题、不搬图、不改绑定。路径策略、逐段拒绝软链接、64 MiB 上限、字节数/哈希/图片签名检查共同约束读取。
- 图片节点及当前放大预览共享临时地址，最多两路读取，节点按实际屏幕交集申请；低缩放不读，离屏或关闭最后一处显示即释放。local-ref 不进入会写回 content 的旧 hydration，原素材 ID/metadata 不变。
- 已完成前端真实源码的 32 项隔离测试、Rust 22 项回归；另用实际读取核心核验案例 4 的 32 张原图共 427,634,172 bytes，大小/哈希/PNG 签名均与登记吻合，数据库副本与绑定前后不变。这不是原生 WebKit/IPC 验收。全量 TypeScript 仍有原先四文件八处错误，图片改动未新增错误。
- 32 张真实原图的大小/哈希/读取及 FFmpeg 完整解码全部通过。仍待原生逐张视觉、缩放占位/离屏内存释放、缺失/越界提示全覆盖；CUA 目录树裁切后未将未看到的节点记为视觉通过。原图节点/放大预览以外的下载、裁剪和参考图工具未扩展，右侧参考缩略图可能仍有问号。

## 项目旧路径与应用登记（第 2 步）

- 用户授权后已备份并修复案例 2/3/4 六处项目 MCP 命令，保留其他配置及三个原绑定文件；从实际新命令读取三个画布成功，分别为 17/10、5/11、48/7 节点/连线。没有启动 AI 模型或改全局账号设置。
- 两处废纸篓旧包已定向注销系统登记，旧包仍可恢复。最终系统重扫后 4 条旧包历史均为 `trash launch-disabled`，只有正式「小陈的画布」可启动；Spotlight 与应用枚举均唯一，无独立 Node 入口。
- 标题兜底不再选中已有或损坏绑定的目录，3 项 Rust 回归通过，保护已安装生效。最终六处路径与三个不变绑定均读回通过；备份、准确路径与实测见 [第 2 步报告](project-path-registration-fix.md)。

## 终端消费确认与失败清理（第 3 步）

- 用户授权的源码修复已正式安装并完成原生复测；详见 [第 3 步报告](terminal-backpressure-fix.md)。每会话未消费预算 256 KiB，单包最多 16 KiB，只在 xterm 消费回调后累计确认；EOF 等排空，异常可见并清理 Channel/会话。
- 真实隔离 PTY 测试发现并修复 macOS 先 wait 后关闭 master/writer 导致的退出等待；空闲、满预算、shutdown、无效 ACK 均验证读取线程、回调、句柄和所测 Shell/前台进程回收。后台未 ACK 不按固定 30 秒杀 Shell，而是有界等待直到消费恢复或明确关闭/出错。
- 前端 55 项通过（新终端协议 19、历史故障复现 4、图片 32），Rust 35 项通过及 1 项 opt-in 跳过；真实 xterm/Channel + 替身 IPC 传输 58,720,286 bytes，哈希一致、最大未消费 262,144 bytes、EOF 1 次、回调零遗留。不是原生 WebKit 完整链路验收；全量 TypeScript 保持原四文件八错。
- 已测高输出输入、拖终端宽度、关闭/重启/退出 App、正常重启及有限时段实际内存；原生后台暂停/恢复、复杂脱离会话进程树、长期压力和全部静默失联仍未保证。普通 Shell、手工启动 AI 和现有登录保持不变。
- 首次原生安装发现 64 MiB 输出约 98.43 秒的吞吐回退；定位真实 macOS PTY 小包与逐包串行确认的往返放大后，完成同消费轮累计确认修复，仍为 256 KiB 预算。第二次安装同参数原生结果为 7.01 秒，尾解析往返 9.006 ms，回退已关闭。真实异步 2 ms 延迟的 2 MiB 隔离对照 ACK 为 2,048→8 次、哈希一致；最终前端 60、Rust 37 项通过（另 1 opt-in 跳过）。前面的 55/35 是首次源码历史。

## 独立 macOS 验收结论

- 整体验收未通过，详见 [独立验收报告](independent-macos-acceptance.md)。本轮接手时新版已安装，未重复安装、未改生产代码或认证。
- 真 App 终端完成 6 轮有限输出、62 次解析往返，无超时；1 MiB 输出尾部 70–126 ms，8 MiB 约 602 ms；512 KiB/s 输出期间程序注入 ABC/回车各接收一次。改标题/切主题保持 Shell，5 轮终端开关及离开画布后未残留测试 Shell。这不是 FPS 或物理键盘到屏幕延迟验收。
- 验收时发现：案例 4 的 32 个原图片文件仍在但原生显示问号；三个真片子六处 MCP 仍指向失效旧 App 路径；隔离故障注入证明约 50 MB 积压可使 xterm 抛错后 Channel 停止推进。当时均未修复；后续顺序修复及待原生验收状态见上方三节。
- 多媒体画布压力、平移/缩放/拖节点帧时间、高输出中拖终端宽度未完成；CUA 原生连接/坐标/文件选择不稳定，测试 ZIP 导入已取消。视口/面板持久化与可见节点更新另有源码隔离复现缺口。
- 数据终验：原 9 行画布所有字段相同，其余 18 表逐行零差异，仅新增一个明确命名的空验收画布；原片子绑定文件哈希未变。WebKit/IndexedDB Blob 未逐项验收。唯一正式 App 与无独立 Node 应用枚举通过，但系统登记仍有两个废纸篓旧包记录。

## 左侧创作目录（第 3 版）

- 用户要求打包更新后，已完成 Next、Agent CLI、Go 与桌面壳构建，并安装到 `~/Applications/小陈的画布.app`，启动显示 1.0.0；构建沿用项目配置跳过全量类型检查。视觉验收状态见根目录 `design-qa.md`。
- 左侧画布列表改为故事设定、视觉设定、分集分镜、其他节点；视觉设定再分人物、场景、道具。按标题关键词归类，未识别的保留在其他节点；每个节点只显示一次，分类内沿用原顺序，不改标题、正文、坐标、画布分组或连线。
- 目录可折叠并显示数量；保留标题、文本正文和提示词搜索，以及全部节点类型筛选。筛选期间自动展开匹配目录，清除后恢复折叠状态；无结果可一键清除筛选。
- 保留点击定位与多选高亮；从画布切换选中节点时展开其所在目录并滚动到对应条目（仍遵循当前搜索条件）。长标题最多两行，悬停可读完整标题和节点类型；状态使用完成、生成中、失败图标及文字标签。
- 使用当前画布浅色/深色主题，左栏采用细分隔线、缩进与轻量选中标记；新建画布默认左栏宽度为 320，已有画布继续使用保存的宽度。资产和提示词库页签保持原逻辑。
- 已现场验证：案例 4 目录共 48 个节点，故事 2、人物 10、场景 17、道具 7、分镜 5、其他 7；人物分类可折叠，搜索 EP02 显示 1 / 48，点击后对应分镜居中并出现选中描边。组合筛选、清除后恢复及选择自动展开仍待完整回归。
- 待测：浅色/深色主题，220 / 280 / 320 / 480 宽度，长中英文标题、空画布、所有节点状态、键盘操作和多选；与第 3 张参考图同视口截图比较。重新打开旧画布后，原数据及保存的面板宽度不变。

- 安装前后 19 张数据库表双向逐行差异均为 0；打开案例并检查后，仅该画布的更新时间变化，全部原节点、连线及其余表内容一致，数据库完整性为 ok。Next / API / Bridge 分别监听本机 3100 / 3101 / 3102，画布页面 HTTP 200，系统应用列表仅有一个画布入口，无独立 Node 应用。
- 备份与核验记录：`../infinite-canvas-backups/local-installs/data-before-outline-20260904-154501/`（相对仓库根目录）；旧 App 备份为同级 `小陈的画布-2026-09-04T07-45-56-019Z-51194.zip`。本次未改版本号、未发布发行版。

## 小陈的画布 1.0.0 与普通终端

- 用户明确要求更新后，源码已构建、签名并安装为「小陈的画布 1.0.0」；独立验收已复核名称、正式入口和后台服务，并完成有限终端场景。其余范围与故障以上方独立验收结论为准，不等同于全部实测通过。
- 用户可见名称改为「小陈的画布」，根版本、桌面包、Rust 包和网页包统一为 1.0.0；版本页读取本机记录，不再请求上游 VERSION/CHANGELOG，旧记录注明上游历史。保留 MIT 许可、内部命令名、identifier 和数据目录。
- 终端取消 Codex/Claude 专属启动按钮、重复上下文按钮和标签栏，只保留目录、清屏、重启及选中节点的可选粘贴入口；不自动运行 AI、不自动发送粘贴内容、不更改各 AI 工具的登录。
- 根因证据：原 PTY 输出同时 emit 和 emit_to；每块字节独立进行有损 UTF-8 解码；ResizeObserver 每次回调都通知 PTY；effect 依赖项目标题且异步启动没有取消保护。
- 输出改成单一有序二进制 Channel，交给 xterm 流式解码；尺寸按帧合并且行列相同时不通知 PTY；标题/主题变化不重启 Shell，关闭或切项目时防止迟到的启动留下旧会话。仅针对这些已定位问题改动，未宣称整体画布性能问题全部解决。
- 已测：隔离 xterm/Channel 的 34 个中文/emoji 分块边界及 1000 行一致；真 App 有限输出、程序注入输入、改标题/切主题和有限终端开关。待测：原生完整输出逐字对账、高输出时拖宽面板、真实帧时间、迟到启动/异常关闭/复杂子进程树及长时间压力。不自动启动模型或消耗 AI 额度。
- 已验证旧名替换、新包严格签名、唯一入口及正式 CLI 链接更新。旧包经 ZIP 校验后进入废纸篓，原数据库与 WebKit 已备份。打开终端后的项目 MCP 路径更新、安装失败回滚仍待对应场景验证。
- 当时安装包保留了 App 登录；后续用户已授权收敛账号功能，当前正式安装和验收状态见本文顶部“本地工作流收敛”。本机 Bridge 鉴权及 AI 工具自身登录保留。
- 实现依据：[Tauri 流式 Channel](https://v2.tauri.app/develop/calling-frontend/#channels) 与 [xterm 流式 UTF-8 输入](https://xtermjs.org/docs/guides/encoding/)。有限流量与解析往返已有测量；绘制和物理输入延迟未验证。后续消费确认式背压已完成源码修复，正式 App 生效与验收以上方第 3 步状态为准。

## 单一应用入口与后台 Node

- 桌面 Node 启动时先加载局部预加载器，阻止 Next 的进程改名触发 macOS 应用登记；仅影响打包子进程，不修改系统 Node。
- 本机安装流程改为身份/签名校验、ZIP 备份完整性验证、旧包移入废纸篓、新包移入唯一正式路径，并定向更新应用登记。拒绝覆盖运行中的 App；安装失败恢复旧包；不修改画布、素材或提示词数据。
- 现场验收完成：4 个重复 App 均已压缩校验并移入废纸篓，旧失效登记已清除；访达应用程序目录、Spotlight 索引和 Launch Services 均只剩正式路径。新版重启后，系统应用列表只有一个无限画布，无 Node/next-server 应用入口，而父子进程及本地服务正常。
- 原有画布、提示词、目录、收藏和素材表与安装前快照双向逐行对比均为 0 差异；提示词接口仍为 1,636 条，数据库完整性为 ok。App 数据目录与 WebKit 已压缩备份并通过完整性检查，旧安装包另存 ZIP 与废纸篓；未清空废纸篓。
- Node 静默回归测试、运行中拒绝覆盖、本机完整打包安装和签名校验已通过；安装异常时恢复旧包的分支尚未做故障注入验证。Spotlight 面板的直接自动化读取连接失败，索引唯一性及访达界面已验证，不冒充该面板截图验收。

## 提示词目录与按需加载

- 空白根因：案例 4 的 10 条记录把标签写成逗号文本，无法按 JSON 数组读取。新增精确修复脚本，仅修改对应 ID 的标签；执行前须备份，正文、标题不变。
- 提示词中心、选词弹窗及画布侧栏改为元数据目录；选中、复制或插入时才读取全文，关闭弹窗释放全文状态。
- “更新在线目录”仅更新元数据，空解析/网络失败保留原目录并显示错误，不再把读取失败显示成 0 条或空列表。批量更新逐来源反馈部分失败。
- 增加“我的本机收藏”，只有明确收藏才新增全文副本，重复收藏幂等；原有历史库保留，未做自动清理。
- 自动测试覆盖目录不泄漏全文、不发起详情网络请求、哈希校验、空源保留、收藏幂等/离线读取/取消、旧库与自建内容不变及搜索筛选。
- 数据库副本与实际页面已验证：1,645 条原内容不变，索引合并 9 条重复后为 1,636 个目录条目；搜索远程案例并查看全文时收藏数为 0，主动收藏后才变为 1；单源更新不增加全文副本；测试服务中断时明确显示读取失败与重试入口。
- 生图工作台选词弹窗已实测：选中本机收藏后，完整文字正确填入输入框；未点击生成、未消耗模型额度。
- `go test ./service ./repository ./handler ./router` 通过。全量 TypeScript 检查仍受原有 4 个未改动文件中的 8 处错误影响（画布资源引用、视频设置、Gemini 正则目标版本、Agent 消息类型），未跨范围修复。
- 已完成用户确认后的本机打包更新：保留旧 App 与两份数据库快照，本机 ad-hoc 签名通过深度严格校验，安装包与构建产物的后端哈希一致；不代表已获得发行签名或公证。
- 正式 App 已验收：目录显示 1,636 条，搜索 Celebrity 返回 3 条且列表不含全文，远程预览图与所选全文正常显示；查看后本机收藏仍为 0。原有 1,645 条提示词和 9 行画布记录与更新前备份逐项相同，数据库完整性为 ok。收藏写入及加载使用在隔离副本验证，测试收藏未进入正式库；仍待用户日常使用确认。

## 画布运行时性能

- 平移/缩放改为直接改世界层 DOM，停手后再回写 React；滚轮已合并到 rAF。
- 拖节点期间只改被拖节点和连线的 transform/path，松手才 `setNodes`。
- 节点 `memo` 的回调和面板改为稳定引用；缩小到 0.2 以下不再解码原图/原视频。
- 打开画布不再一次性 hydrate 全部媒体，只加载视口内节点；视频 `preload="none"`，同时只允许一条在播。
- 画布 persist 按项目分片写入 IndexedDB，视口/侧栏改动不改 `updatedAt`、不触发云端全量 PUT。
- 独立核验发现当前桌面仍是等 Go 就绪后才启动 Node，不能沿用“并行启动”的旧描述。切到后台标签会停任务轮询；助手面板动态加载；`ProConfigProvider` 只留在管理后台；实际启动和界面性能收益须分别测量。

## 本机 Agent 适配层

- 新增只监听 `127.0.0.1:3102` 的 Agent Bridge，以及桌面安装专属、
  `0600` 保存、可立即撤销替换的本机凭据。
- 新增 `infinite-canvas` CLI：能力目录、项目列表/读取、画布操作
  dry-run/apply、运行时探测、任务状态/取消和零付费确定性测试片。
- Agent 写入带 project/request/base revision/actor，使用 request journal 幂等；revision 冲突和人工锁定节点均拒绝覆盖。
- 桌面 WebView 改由 Tauri IPC 与 Agent 共用现有 SQLite
  `canvas_projects` 表，首次加载会合并原 IndexedDB 项目。
- 自动化已覆盖 loopback、鉴权、撤销、白名单、路径 schema、幂等、JSON 和
  CLI 退出码；总装后的签名 `.app` 已确认 CLI 打包位置、首次项目合并和真实片子目录绑定。
- 已完成 MCP STDIO 实测：四个工具可发现，`canvas_context` 读取案例2为 17 节点/10 连线，单节点 `canvas_read` 只返回该节点，`canvas_mutate` dry-run 后数据库数量不变。
- 已完成右侧本地终端实测：案例2目录自动连接画布，选中节点会出现上下文标签并以安全粘贴方式进入输入区；Codex 能在侧栏启动并带入无限画布 MCP 配置。
- 仍需后续长时间观察多人/多 Agent 连续编辑；Claude Code 新项目 MCP 的首次信任确认由用户在首次使用时完成。

## macOS Tauri 2 桌面壳

- 新增 Apple Silicon macOS 桌面构建，复用现有 Next.js standalone 和 Go API，不改写 React 画布。
- 桌面版 Node/Go sidecar 只监听 `127.0.0.1`，参数和资源路径由 Rust 固定，网页 capability 不包含 shell 权限。
- SQLite、后端日志和 WebKit 画布数据使用应用专属目录；开发模式和生产 `.app` 已完成本机启动、退出重启及项目恢复验证。
- 画布已用确定性测试素材完成本地 FFmpeg 任务回填、文本/图片/视频/音频节点、来源连线、ZIP 导出回灌、播放、重启恢复和重复防护验收。
- 现阶段仅供本机开发验收；技术 DMG 已生成并校验完整，但本机缺少 Developer ID Application 身份，发行签名、公证、stapling、Gatekeeper 干净安装与覆盖升级被精确阻塞。
- Node sidecar 的最小发行 entitlement 已验证为 `com.apple.security.cs.allow-jit`；最终签名必须移除开发态 `get-task-allow`，其余主程序、Go 与 Sharp 组件不需要额外 entitlement。

## 七项优化：本轮数据与请求实现

需求与画质边界见 [PRD 第 9 节](../overview/product-requirements.md#9-已批准的七项优化)。

- 持久版本历史：列表、时间、节点/关系/对话差异、恢复前保留及产生新修订；人工/Agent 事务接入、恢复经串行保存与预览修订校验。副本重启、冲突、故障和幂等测试通过，待正式历史面板与撤销重做验收。
- 资料库数据库分页：真实副本 26 组新旧接口相同；20,000 条扩展目录与素材、OR 标签/中文/收藏/边界页/并发更新通过。性能只记录隔离数据库结果，不宣称正式 App 提速。
- 请求取消与读取超时：界面信号→代理→异步 Rust 媒体流，区分取消、连接失败、读取超时及流意外中断；写入等待取消保留可能已提交提示及请求编号，不自动重发。前端 8 项和 Rust 流回收/原字节/超时测试通过，待原生端到端。
- 前端四项已完成并安装：相邻连线索引与跨边界挂载；长对话每页最多 12 条、全文查找和复制；旧 URL 及素材库消费者租用/释放、使用中保护；实际节点/SOP SHA-256/后续工具返回清单。专项 84 项隔离测试、全量 TypeScript 通过，尚无正式 App 性能结论。活动页面为原图编辑持有的字节没有硬上限。详见 [前端专项报告](canvas-ui-optimization.md)。

详见 [数据专项报告](canvas-data-optimization.md)。正式构建和安装已由主整合完成，原生剩余项见顶部；原素材与既有恢复备份均保留。
