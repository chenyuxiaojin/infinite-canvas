# 顺序修复与正式 App 验收

## 最终交付状态

2026-09-04：三项修复已按顺序安装到正式「小陈的画布 1.0.0」，最高模型独立专项测试与主任务原生复测均已执行。原生 64 MiB 输出从首轮背压版 98.43 秒降至累计确认版 7.01 秒；这约 14 倍的差异仅指此受控输出生产耗时，不是整个画布或模型响应提速。原图节点/放大预览已抽验，旧路径已修复，可启动 App 入口唯一。下文明确保留未覆盖场景，不作为全部功能或长期性能验收。

## 范围

按用户授权顺序完成图片读取、旧项目路径/应用登记、终端背压修复。复用用户指定最高模型的独立任务进行源码与专项回归，主任务执行正式安装和原生检查。未启动 AI 模型，未更改 AI 工具账号、Bridge 凭据、真实片子的节点/连线或原图。登录/云端功能不属于本轮删除范围。

## 图片

- 32 张原图均存在于既有 App 的 `agent-media/verified`，共 427,634,172 bytes；受限读取核心逐项核对登记大小、SHA-256、格式通过，另以 FFmpeg 完整解码 32/32 通过。没有生成、搬动或覆盖图片。
- 正式 App 中，实际看到杰克、乔治、格拉迪丝定妆照，以及西柏林街头、WISCONSIN 酒馆、厨房、格鲁吉亚废墟的画面；杰克原图的放大预览亦通过 CUA 截图读回。不是以占位图或仅存在文件判断显示成功。
- 目录共识别 32 个图片节点，自动逐项点击完成前 3 个后，原生辅助功能树裁切了目录（`showing 17-117 of 117 items`），未把剩余节点的逐张原生视觉检查记为通过。已有文件/读取/完整解码覆盖全部 32 张，原生视觉为上述抽验。
- 本轮只接通图片节点和放大预览。参考图栏、裁剪、下载/导出等直接使用 local-ref 的其他工具仍未扩展；右侧 Agent 的参考缩略图仍可能显示问号。没有把这些入口或画布帧率记为全部通过。

实现与专项证据见 [图片修复](local-ref-image-fix.md)。

## 项目连接与应用登记

- 案例 2/3/4 的 6 处旧命令已按自动化审计流程先备份、精确修正、实际连通和回读；其他配置保留，3 个绑定文件逐字未变。实际读取分别为 17/10、5/11、48/7 节点/连线，案例 4 仍是 `DUkqxVcwRh30uwMAskyxt`，未按同名旧画布改绑。
- 标题候选排除已有及损坏绑定，3 项回归通过。正式路径始终为 `/Users/chenhuajin/Applications/小陈的画布.app`，版本 1.0.0，identifier 与数据目录不变。
- 两个废纸篓旧包只注销系统登记，没有删除备份、清空废纸篓或重置系统应用数据库。最终系统重扫后出现 4 条旧包历史记录，均明确标为 `trash launch-disabled`；仅正式 App 为可启动登记。Spotlight 只返回正式路径，CUA 应用枚举只有「小陈的画布」，没有独立 Node/next-server 应用。不能把“可启动入口唯一”写成“系统数据库完全没有旧包记录”。

详情与准确备份路径见 [项目连接修复](project-path-registration-fix.md)。

## 终端首轮正式测试与发现的吞吐回退

首次背压版主程序 SHA-256：`f1aeb3af1cf5ceb22bf3df488c7c15c2490558f851014e9a635337d27d620e53`。签名深度严格校验通过，内部 API/CLI 二进制与安装前哈希相同。下面结果仅代表这次首轮原生测试，不冒充后续聚合确认版结果。

| 原生测试 | 有限输出 | 生产耗时 | 结果 |
| --- | ---: | ---: | --- |
| 小突发 | 1,048,542 bytes | 666.61 ms | 无错，3 次解析往返成功 |
| 大突发 | 67,108,800 bytes | 98,434.70 ms | 无错、无卡死，完成后仍能响应；吞吐明显回退，重新打开第 3 步优化 |
| 持续输出 | 7,864,296 bytes / 15 秒 | 15,000.37 ms | 每秒 512 KiB；XYZ/回车在第 4.8 秒各接收一次，解析往返均成功、最大 14.942 ms |

- 大突发过程中，终端右侧面板从约 650 调窄至约 470 点，输出继续，原生截图可见尺寸变化。一次恢复宽度操作遇到 CUA 坐标错误，未将其记为产品拖动失败或已恢复原宽度；只影响空测试画布的 UI。
- 大突发的 ABC/回车在生产循环结束后才由探针处理：该探针连续同步 TTY 写入会延后自身读取输入，不能把它解释为 98 秒的 App 输入延迟；因此另外完成上面的定速流式输入测试。
- DSR 是 PTY→Tauri→xterm 解析→PTY 的往返，不是绘制完成、物理按键延迟或 FPS。64 MiB 未逐行视觉对账；逐字节哈希证明来自独立真实 xterm/Channel 专项测试，两个证据层次不能混淆。
- 60 秒原生采样只覆盖大突发的前一部分：主 App RSS 126.45→127.41 MiB、峰值 127.42 MiB；WebContent RSS 386.67→659.84 MiB、峰值 660.69 MiB；Go/Node 采样 CPU 为 0%。不能据此判断长期泄漏或全部画布流畅。
- 输出中关闭测试：准确 Shell PID 69081 与前台测试 Node 70379 均消失，App 仅剩 Go/Node；重开生成 Shell 70440，随后正常退出 App，主 68914、Go 68917、Node 68918、Shell 70440 全部消失，3100–3102 无监听。未触碰其他 Agent。

首轮测试发现小包逐个串行 ACK 会放大原生往返开销，已返回源码完成累计确认修复，而不是将“没卡死”直接算作性能完成。

## 累计确认版：最终正式安装与同参数复测

最终主程序 SHA-256 为 `aa88a626909ae4594994c1e83c4071af920b200be0f98891218f4fe80f6a4b4f`，严格深度签名验证通过。版本仍为 1.0.0，API/CLI 哈希与首轮及原安装前相同，功能代码冻结后重新构建安装。正式路径未变，构建目录不留第二个 App。原生测试仍在既有空 QA 画布 `YD3Si4Ubw0nXbcEjvVq38`，未新建画布；终端初始宽度约 650 点，与首轮开始时相同。

| 原生测试 | 实际输出 | 生产耗时 | 尾部/持续解析往返 |
| --- | ---: | ---: | --- |
| 1 MiB 突发 | 1,048,542 bytes | 113.18 ms | 尾部 28.113 ms，3/3 成功 |
| 64 MiB 突发 | 67,108,800 bytes | 7,010.67 ms | 尾部 9.006 ms，3/3 成功 |
| 15 秒持续输出，512 KiB/s | 7,864,296 bytes | 14,999.95 ms | 22/22 成功，最大 24.007 ms |

- 三次均 `aborted=false`、`error=null`。持续输出时 XYZ/回车在第 5.17–5.20 秒各接收一次，没有运行 AI。64 MiB 末尾原生截图显示 `QA_PAYLOAD_END rows=1016800 bytes=67108800`；这仍不是百万行逐行视觉对账或 FPS 测量。
- 另启 20 秒有限流式输出，在输出约第 35,745 行时将右栏从约 470 拖宽到 650 点，截图显示界面更新和输出继续；随后在输出中关闭面板。Shell 71724、前台 Node 72120 均消失，没有假定探针被取消后应生成正常完成报告。
- 重开终端产生 Shell 72225，正常退出 App 后主 71653、Go 71668、后台 Node 71669、Shell 72225 均消失，3100–3102 无监听。之后重新打开正式 App，留在首页；系统应用枚举只返回一个「小陈的画布」。
- 30 秒资源采样覆盖完整 64 MiB 输出及随后空闲：主 App RSS 141.36→142.55 MiB、峰值 142.66 MiB；WebContent 260.17→408.95 MiB、峰值 409.42 MiB；累计 CPU 分别 7.42 与 9.34 秒。Go/Node 无采样 CPU。前后浏览图片、冷/热缓存和采样时长不同，不用这些数字直接宣称内存优化比例或没有长期泄漏。
- 最终源码回归：前端 60 项、Rust 37 项通过，另 1 项真实原图 opt-in 默认跳过（此前已单独跑过 32 张）。256 KiB 未消费上限不变；隔离完整字节哈希、单在途确认、EOF、异常和关闭回归通过。TypeScript 的原四文件八错未在本轮跨范围修复。
- 未完成：真正后台窗口暂停/恢复的原生压力、长期运行/复杂脱离会话子进程、多媒体平移缩放帧时间、32 张逐张原生视觉与所有图片辅助入口。普通 Shell 保留，账号/云端入口未动。

最终报告：`terminal-probe-2026-09-04T09-45-07-704Z-71739.json`、`terminal-probe-2026-09-04T09-45-21-242Z-71786.json`、`terminal-probe-2026-09-04T09-46-10-091Z-72018.json`；资源采样 `process-repair-aggregate-burst-2026-09-04T09-45-16.021Z.json`，均在下面的私有证据目录。

## 数据与可恢复备份

私有证据目录：`/Users/chenhuajin/项目/自己的应用/infinite-canvas-backups/local-installs/ordered-repair-92iTyc/`。

- `before-repair.db`、`after-step2.db`：19 表全字段排序哈希相同、10 个画布相同，schema 相同，完整性均 `ok`。
- `after-first-native.db` / `first-native-data-comparison.json`：18 张非画布表全部相同；案例 4 仅 `updatedAt` 和正常定位操作改变的 `viewport`，空 QA 画布仅 `updatedAt`；节点、连线和其余原记录不变。没有新增或删除正式画布。
- `after-final-native.db` / `final-native-data-comparison.json`：最终退出后的全量复核仍为上述两种 UI/更新时间变化；18 张非画布表相同、10 个画布无增删、所有节点/连线相同，schema 相同，完整性均为 `ok`。六处 MCP 命令仍为正式路径，三个原绑定逐字相同。
- `webkit-before-install.zip`：正常退出 App 后备份并完整校验；6 个 MCP 配置与3个片子绑定按原目录结构保存，均在修改前逐字核验。
- 首轮旧 App ZIP 为同级 `小陈的画布-2026-09-04T09-32-10-176Z-68472.zip`，完整校验通过；旧包在对应 `.Trash/小陈的画布-替换前-2026-09-04T09-32-10-176Z-68472.app` 可恢复。没有留下构建目录中的第二个 App。
- 最终安装另保存完整校验通过的 `小陈的画布-2026-09-04T09-44-29-993Z-71076.zip`；对应旧包移入 `.Trash/小陈的画布-替换前-2026-09-04T09-44-29-993Z-71076.app`，仍可恢复，未永久删除。
- 原生探针报告为该证据目录内 `terminal-probe-2026-09-04T09-35-14-017Z-69114.json`、`09-35-44-588Z-69205.json`、`09-38-41-077Z-70154.json`；资源采样为 `process-repair-large-burst-2026-09-04T09-35-38.579Z.json`。探针新增明确的 `--large-burst` 开关才允许最多 64 MiB，默认仍为 8 MiB。
- SQLite 核对不等于逐项比较 WebKit/IndexedDB Blob；仅提供其可恢复 ZIP，不宣称所有浏览器本地媒体逐项验收。
