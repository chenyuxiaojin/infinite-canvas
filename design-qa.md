# 左侧创作目录 · 第 3 版

- 参考图：`/Users/chenhuajin/.codex/generated_images/01a06b22-3828-77d2-8c05-3ad4e55e57e3/exec-a1bbe10e-ca5d-4d5a-8f17-1cb37a165185.png`，1487 × 1058 像素。
- 实现：`/Users/chenhuajin/Applications/小陈的画布.app`，1.0.0；案例 `DUkqxVcwRh30uwMAskyxt`。
- 源码入口：`web/src/app/(user)/canvas/components/canvas-node-outline.tsx`。
- 本轮已完成用户授权的构建、安装、严格签名校验及启动。原包、应用数据和 WebKit 均已备份并验证。

## 已取得的运行证据

- 原生窗口中可见新左栏及 1.0.0 版本；通过本次会话 CUA 截图呈现，工具未返回可持久引用的截图路径。
- 原生截图曾取得 1229 × 768 JPEG；另一张为展开窗口状态，尚未完成 CSS 视口和密度归一化。实际保留旧画布 220 宽度，与图稿宽度不同，不能据此宣称像素一致。
- 初始目录 48 节点：故事 2、人物 10、场景 17、道具 7、分镜 5、其他 7；人物折叠状态变化已读回。
- 搜索 EP02 显示 1 / 48；点击对应条目后，画布分镜出现蓝色选中描边并定位显示。
- 所有原节点及连线与更新前逐项相同。目录仅改变展示，真实标题和内容保留。

## 视觉检查状态

- 字体和排版：已看到中文标题、两行截断与分类层级；字体密度和完整窄宽度回归未完成。
- 间距和布局：缩进、细分隔线和轻量选中标记可见；未完成与参考图相同视口的比较。
- 颜色：深色主题已显示；浅色状态尚未现场验收。继续使用既有画布主题。
- 素材：使用已有 Lucide 图标，无新增栅格素材；画布原图和文本沿用原数据。
- 文案：分类标签和真实节点标题已读回；图稿示意标题未写入正式画布。
- 全幅与局部联合对照：未完成；不能把分别看过的截图当成联合比较通过。
- 比较历史：没有完成有效的归一化比较轮次，因此不作无依据的样式修正。

## 阻塞与后续

原生 CUA 在继续界面验收时出现 `Sky Computer Use native pipe closed before response`；重新连接后又返回 ScreenCaptureKit `-3811`（音频/视频捕捉失败，无法开始流播放）。截图还曾落在 macOS 窗口动画中间帧。应用本身仍运行，本地服务与数据正常；该问题不能作为应用故障的证据。

待原生捕捉恢复后，在相同主题、视口与展开状态下，把参考和实际截图放在同一个比较输入中复核；继续检查清除筛选、组合类型筛选、选择自动展开、多选、浅色、空状态和窄宽度。剩余终端专项验收见 `docs/progress/pending-test.md`。

final result: blocked

阻塞范围为完整视觉验收及剩余交互回归；打包更新、启动和上述已列功能验证已完成。
